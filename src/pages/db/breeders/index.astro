---
import Layout from '../../../layouts/Layout.astro';
import StrainsByBreeder from '../../../components/db/StrainsByBreeder';

const pageTitle = "Base de Datos de Cepas por Breeder";
const pageDescription = "Explora nuestra extensa colección de cepas de cannabis organizadas por breeder. Encuentra información detallada sobre genéticas, tiempos de floración y más.";
---

<Layout title={pageTitle} description={pageDescription}>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="text-center">
            <h1 class="text-4xl font-extrabold text-gray-900 sm:text-5xl md:text-6xl">
                Base de Datos de Cepas
            </h1>
            <p class="mt-3 max-w-md mx-auto text-base text-gray-500 sm:text-lg md:mt-5 md:text-xl md:max-w-3xl">
                Explora nuestra colección completa de cepas organizadas por breeder. Encuentra información detallada sobre cada variedad.
            </p>
        </div>

        <div class="mt-12">
            <div class="bg-white rounded-lg shadow-sm">
                <StrainsByBreeder client:load />
            </div>
        </div>

        <div class="mt-8 text-center text-sm text-gray-500">
            <p>Los datos son actualizados regularmente para mantener la información precisa y al día.</p>
        </div>
    </div>
</Layout>

<style>
    h1 {
        background: linear-gradient(to right, #22c55e, #16a34a);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }
</style>

